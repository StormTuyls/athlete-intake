/**
 * Evalharnas. M3.
 *
 * Draait de fixtures uit evals/fixtures door de extractiepijplijn en diff't het
 * resultaat tegen evals/expected. Loopt in CI op elke promptwijziging, want een
 * prompt aanpassen is een gedragswijziging zonder compilerfout.
 */
async function main() {
  throw new Error("niet geïmplementeerd: M3");
}

void main();
